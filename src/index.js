import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// react applicaton can render only one component !!!
// everything should be in application !!!
ReactDOM.render(<App />, document.getElementById('root')); 

